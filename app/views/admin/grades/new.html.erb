<% content_for :title, "New Grade" %>

<section class="page-heading">
  <div>
    <h1>New grade</h1>
    <p>Record a grade for a student in a specific unit.</p>
  </div>
  <%= link_to "Back to grades", admin_grades_path %>
</section>

<section class="card">
  <%= render "form", grade: @grade, students: @students, units: @units, submit_label: "Create grade" %>
</section>
