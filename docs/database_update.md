# Database updates 

- `domains` table has been removed.
- `curricula` and `curriculum_modules` have been renamed to `formation_plan` and `formation_plan_modules`.
- `evaluations` table has been removed -> `grades` are linked between `units` and `students`.